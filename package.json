{
  "name": "autosprint-root",
  "version": "1.0.0",
  "description": "Root configuration to run both Spring Boot backend and Vite frontend concurrently.",
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
